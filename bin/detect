#!/usr/bin/env bash
# bin/detect <build-dir>
BUILD_DIR=$1
cd $BUILD_DIR

## echo "TCL" and exit with status of 0 if there is a .tcl file in
## the top level directory.
ls -U *.tcl > /dev/null 2>&1 && (echo "TCL") || exit 1
